#!/usr/bin/env bash

BUILD_DIR=$1
CACHE_DIR=$2

NODE_SETUP=$CACHE_DIR/nodesource_setup.sh

curl -sL https://deb.nodesource.com/setup_8.x -o $NODE_SETUP
chmod a+x $NODE_SETUP
$NODE_SETUP
apt-get install nodejs
node -v

exit 0