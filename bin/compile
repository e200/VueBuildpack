#!/usr/bin/env bash

CACHE_DIR=$2

curl -sL https://deb.nodesource.com/setup_8.x -o $CACHE_DIR/nodesource_setup.sh
cat $CACHE_DIR/nodesource_setup.sh
$CACHE_DIR/nodesource_setup.sh
apt-get install nodejs
node -v

exit 0